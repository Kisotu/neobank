openapi: 3.0.3
info:
  title: Banking API
  version: 0.1.0
  description: Initial API scaffold for the banking application.
servers:
  - url: http://localhost:8080
paths:
  /health:
    get:
      summary: Health check
      operationId: healthCheck
      responses:
        "200":
          description: Service is healthy
          content:
            text/plain:
              schema:
                type: string
                example: ok